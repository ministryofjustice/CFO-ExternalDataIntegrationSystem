CREATE TABLE [search].[Precedence] (
    [Identifier] NVARCHAR (10) NOT NULL,
    [LastName]   NVARCHAR (10) NOT NULL,
    [DOB]        NVARCHAR (10) NOT NULL,
    [Blockable]  BIT           NOT NULL,
    [Precedence] SMALLINT      NOT NULL
);

